# -*- mode: snippet -*-
# key: nost
# name: stateless
# --
import React from 'react';
import * as styles from './${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}.less'

const ${2:name}=(${3:props})=>(
 <${4:div}>
    $0
  </$4>
)

export default $2;